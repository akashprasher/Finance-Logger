var chars = 'repository';
console.log(chars);
var inputs = document.querySelectorAll('input');
inputs.forEach(function (input) {
    console.log(input);
});
// 'tsc sandbox.ts -w' can be used to compile ts autimatically.
